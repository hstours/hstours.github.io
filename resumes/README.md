# Resumes

Store resume PDFs and other resume versions in this folder.

The current download button points to `resume.pdf` in the repository root. Update that link in `index.html` to point to a file here when you add a resume, for example `resumes/henry-smith-resume.pdf`.